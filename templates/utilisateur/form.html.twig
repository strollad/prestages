{% extends 'base.html.twig' %}

{% block body %}
    <h3 class="page-header">Modifier l'utilisateur</h3>

    <div class="row">
        <div class="col-sm-offset-1 col-sm-10">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}